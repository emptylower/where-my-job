{
  "schema_version": 1,
  "stream": "custom.interviews",
  "stream_revision": 2,
  "subject_kind": "application",
  "types": {
    "scheduled": {"required": ["round", "at", "format"], "fields": {"round": "string", "at": "datetime", "format": "string"}},
    "done":      {"required": ["round"],       "fields": {"round": "integer", "questions": "string", "self_rating": "integer"}}
  }
}
